<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Review">  
  	<select id="ReviewList"
  		resultType="com.green.restaurant.mypage.review.Vo.MypageReviewVo">
  		SELECT
  			BOARD_TITLE,
			BOARD_CONTENT,
			CNT_LIKE,
			VIEW_CNT,
			BOARD_IDX,
			USER_ID
		FROM
			BOARD
		WHERE
			USER_ID=#{userId} AND BOARD_TYPE=2
		ORDER BY
			VIEW_CNT DESC
  	</select>
  </mapper>